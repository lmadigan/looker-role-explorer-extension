import React, { Children, cloneElement } from 'react';
import styled from 'styled-components';
export const TabList = ({ children, selectedIndex, onSelectTab, }) => {
    const clonedChildren = Children.map(children, (child, index) => {
        return cloneElement(child, {
            onSelect: () => onSelectTab && onSelectTab(index),
            selected: index === selectedIndex,
            selectedIndex,
        });
    });
    return React.createElement(TabListContainer, null, clonedChildren);
};
const TabListContainer = styled.div.withConfig({ displayName: "TabListContainer", componentId: "sc-1w7fhe1" }) `
  border-bottom: 1px solid ${props => props.theme.colors.palette.charcoal200};
`;
//# sourceMappingURL=TabList.js.map