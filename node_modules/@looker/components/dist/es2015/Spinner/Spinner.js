import { position, space, reset, } from '@looker/design-tokens';
import omit from 'lodash/omit';
import range from 'lodash/range';
import React from 'react';
import styled from 'styled-components';
import { SpinnerMarker } from './SpinnerMarker';
const SpinnerFactory = props => {
    const { color = 'palette.charcoal900', markers = 13, markerRadius, speed = 1000, } = props;
    return (React.createElement(Style, Object.assign({}, omit(props, 'color', 'markers', 'markersRadius', 'speed')), range(markers).map(i => (React.createElement(SpinnerMarker, { backgroundColor: color, key: i, speed: speed, markers: markers, markerIndex: i, markerRadius: markerRadius })))));
};
const Style = styled.div.withConfig({ displayName: "Style", componentId: "sc-1bvng2k" }) `
  ${reset}
  ${space}
  ${position}

  height: ${props => props.size}px;
  position: relative;
  width: ${props => props.size}px;
`;
Style.defaultProps = {
    size: 30,
};
export const Spinner = styled(SpinnerFactory).withConfig({ displayName: "Spinner", componentId: "sc-1ogbic8" }) ``;
//# sourceMappingURL=Spinner.js.map