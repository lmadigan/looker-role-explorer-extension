import React from 'react';
import { ModalManager } from '../ModalManager';
import { Drawer } from '.';
export class DrawerManager extends ModalManager {
    renderModal(content, props) {
        return (React.createElement(Drawer, Object.assign({ isOpen: this.state.isOpen, onClose: this.close }, props), content));
    }
}
//# sourceMappingURL=DrawerManager.js.map