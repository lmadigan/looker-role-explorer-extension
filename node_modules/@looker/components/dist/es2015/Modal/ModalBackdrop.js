import { reset } from '@looker/design-tokens';
import { color, } from 'styled-system';
import styled from 'styled-components';
export const ModalBackdrop = styled.div.attrs((props) => ({
    backgroundColor: props.visible ? props.backgroundColor : 'transparent',
})).withConfig({ displayName: "ModalBackdrop", componentId: "sc-c89ych" }) `
  ${reset}

  ${color}
  cursor: default;
  opacity: ${props => props.opacity || 0.4};
  transition: opacity ${props => props.theme.transitions.durationSimple};
  ${props => props.inlineStyle};

  bottom: 0;
  left: 0;
  right: 0;
  top: 0;
  position: fixed;

  &.entering,
  &.exiting {
    opacity: 0.01;
  }
`;
ModalBackdrop.defaultProps = {
    backgroundColor: 'palette.charcoal200',
    opacity: 0.6,
    visible: true,
};
//# sourceMappingURL=ModalBackdrop.js.map