var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { useToggle } from '../../utils/useToggle';
import { ConfirmationDialog } from './ConfirmationDialog';
export function useConfirm(props) {
    const { value, setOn, setOff } = useToggle();
    const rendered = (React.createElement(ConfirmationDialog, Object.assign({}, props, { isOpen: value, close: setOff })));
    return [rendered, setOn];
}
export const Confirm = (_a) => {
    var { children } = _a, props = __rest(_a, ["children"]);
    const [confirmation, confirm] = useConfirm(props);
    return (React.createElement(React.Fragment, null,
        children(confirm),
        confirmation));
};
//# sourceMappingURL=Confirm.js.map