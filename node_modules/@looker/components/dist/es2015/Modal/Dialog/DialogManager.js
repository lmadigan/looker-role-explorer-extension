import React from 'react';
import { ModalManager } from '../ModalManager';
import { Dialog } from '.';
export class DialogManager extends ModalManager {
    renderModal(content, props) {
        return (React.createElement(Dialog, Object.assign({ isOpen: this.state.isOpen, onClose: this.close }, props), content));
    }
}
//# sourceMappingURL=DialogManager.js.map