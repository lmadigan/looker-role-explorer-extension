var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useCallback } from 'react';
import { Button, ButtonTransparent } from '../../Button';
import { Heading } from '../../Text/Heading';
import { Paragraph } from '../../Text';
import { ModalContent, ModalFooter, ModalHeader } from '../Layout';
import { Dialog } from './Dialog';
export const ConfirmationDialog = (_a) => {
    var { cancelLabel = 'Cancel', close, confirmLabel = 'Confirm', buttonColor = 'primary', isOpen = false, message, onCancel, onConfirm, title } = _a, props = __rest(_a, ["cancelLabel", "close", "confirmLabel", "buttonColor", "isOpen", "message", "onCancel", "onConfirm", "title"]);
    const confirm = useCallback(() => {
        onConfirm(close);
    }, [close, onConfirm]);
    const cancel = useCallback(() => {
        if (onCancel) {
            onCancel(close);
        }
        else {
            close();
        }
    }, [close, onCancel]);
    return (React.createElement(Dialog, Object.assign({ isOpen: isOpen, onClose: cancel }, props),
        React.createElement(ModalHeader, null,
            React.createElement(Heading, null, title)),
        message && (React.createElement(ModalContent, null,
            React.createElement(Paragraph, null, message))),
        React.createElement(ModalFooter, null,
            React.createElement(Button, { onClick: confirm, color: buttonColor }, confirmLabel),
            React.createElement(ButtonTransparent, { onClick: cancel }, cancelLabel))));
};
//# sourceMappingURL=ConfirmationDialog.js.map