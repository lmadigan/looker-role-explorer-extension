import { reset, space, typography, position, layout, } from '@looker/design-tokens';
import styled from 'styled-components';
import { variant } from 'styled-system';
const typeVariant = variant({
    prop: 'type',
    variants: {
        bullet: {
            listStyleType: 'disc',
            pl: 'medium',
        },
        letter: {
            listStyleType: 'upper-alpha',
            pl: 'medium',
        },
        none: {
            listStyleType: 'none',
        },
        number: {
            listStyleType: 'decimal',
            pl: 'medium',
        },
    },
});
export const List = styled.ul.attrs((props) => ({
    as: ['letter', 'number'].includes(String(props.type)) ? 'ol' : undefined,
    type: props.nomarker ? 'none' : props.type,
})).withConfig({ displayName: "List", componentId: "sc-1dd290l" }) `
  ${reset}
  ${typography}
  ${typeVariant}
  ${space}

  ${position}
  ${layout}
`;
List.defaultProps = { type: 'none' };
//# sourceMappingURL=List.js.map