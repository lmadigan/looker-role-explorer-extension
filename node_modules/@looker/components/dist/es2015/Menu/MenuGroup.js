var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import styled from 'styled-components';
import { color, reset, space, } from '@looker/design-tokens';
import { List } from '../List';
import { MenuGroupLabel } from './MenuGroupLabel';
import { cloneMenuListChildren } from './MenuList';
const MenuGroupInternal = (_a) => {
    var { children, label, labelProps, labelStyles } = _a, props = __rest(_a, ["children", "label", "labelProps", "labelStyles"]);
    const { customizationProps, compact } = props, boxProps = __rest(props, ["customizationProps", "compact"]);
    const clonedChildren = cloneMenuListChildren(children, {
        compact,
        customizationProps,
    });
    return (React.createElement(MenuGroupWrapper, Object.assign({}, boxProps, { backgroundColor: customizationProps && customizationProps.bg, py: "small" }),
        label && (React.createElement(MenuGroupLabel, Object.assign({ backgroundColor: customizationProps && customizationProps.bg, labelStyles: labelStyles, labelContent: label }, labelProps))),
        React.createElement(List, { nomarker: true }, clonedChildren)));
};
const MenuGroupWrapper = styled.li.withConfig({ displayName: "MenuGroupWrapper", componentId: "sc-1bz76uo" }) `
  ${reset}
  ${space}
  ${color}
`;
export const MenuGroup = styled(MenuGroupInternal).withConfig({ displayName: "MenuGroup", componentId: "sc-by1v24" }) ``;
//# sourceMappingURL=MenuGroup.js.map