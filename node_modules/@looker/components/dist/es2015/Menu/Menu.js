import React, { Children, cloneElement, useRef, useState, } from 'react';
export function useMenu(disabled, initialIsOpen = false) {
    const triggerRef = useRef(null);
    const [isOpen, setOpen] = useState(initialIsOpen);
    return { disabled, isOpen, setOpen, triggerRef };
}
export const Menu = ({ children, disabled, isOpen }) => {
    const menu = useMenu(disabled, isOpen);
    const cloned = Children.map(children, (child) => {
        return cloneElement(child, menu);
    });
    return React.createElement(React.Fragment, null, cloned);
};
//# sourceMappingURL=Menu.js.map