const getTabStops = (ref) => Array.from(ref.querySelectorAll('a,button,[tabindex="0"]'));
export const moveFocus = (direction, initial, containerRef) => {
    if (!containerRef || !containerRef.current)
        return;
    const tabStops = getTabStops(containerRef.current);
    if (document.activeElement &&
        tabStops.includes(document.activeElement)) {
        const next = tabStops.findIndex(f => f === document.activeElement) + direction;
        if (next === tabStops.length)
            return;
        if (!tabStops[next])
            return;
        tabStops[next].focus();
    }
    else {
        tabStops.slice(initial)[0].focus();
    }
    return false;
};
//# sourceMappingURL=moveFocus.js.map