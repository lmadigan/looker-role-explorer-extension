import { palette } from '@looker/design-tokens';
const initial = {
    bg: palette.white,
    color: palette.charcoal600,
    fontSize: 'small',
    fontWeight: 'normal',
    iconColor: palette.charcoal300,
    iconSize: 20,
};
const hover = Object.assign(Object.assign({}, initial), { bg: palette.charcoal100, color: palette.charcoal900 });
const current = Object.assign(Object.assign({}, hover), { fontWeight: 'bold', iconColor: palette.charcoal900 });
export const defaultMenuItemStyle = {
    current,
    hover,
    initial,
    marker: {
        color: palette.charcoal900,
        size: 4,
    },
};
//# sourceMappingURL=menuItemStyle.js.map