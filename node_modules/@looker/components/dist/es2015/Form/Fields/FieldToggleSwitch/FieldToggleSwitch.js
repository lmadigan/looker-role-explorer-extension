import React, { forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { useFormContext } from '../../Form';
import { ToggleSwitch, } from '../../Inputs/ToggleSwitch/ToggleSwitch';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldToggleSwitchComponent = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const { id = uuid() } = props;
    return (React.createElement(Field, Object.assign({ id: id, alignLabel: "right", alignValidationMessage: "bottom", validationMessage: validationMessage }, pickFieldProps(props)),
        React.createElement(ToggleSwitch, Object.assign({}, omitFieldProps(props), { id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldToggleSwitchComponent.displayName = 'FieldToggleSwitchComponent';
export const FieldToggleSwitch = styled(FieldToggleSwitchComponent).withConfig({ displayName: "FieldToggleSwitch", componentId: "sc-1gsx5as" }) ``;
//# sourceMappingURL=FieldToggleSwitch.js.map