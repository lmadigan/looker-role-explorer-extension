import React, { forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { useFormContext } from '../../Form';
import { InputText } from '../../Inputs/InputText/InputText';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldTextComponent = forwardRef((props, ref) => {
    const { id = uuid() } = props;
    const validationMessage = useFormContext(props);
    return (React.createElement(Field, Object.assign({ id: id, alignValidationMessage: "bottom", validationMessage: validationMessage }, pickFieldProps(props)),
        React.createElement(InputText, Object.assign({}, omitFieldProps(props), { id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldTextComponent.displayName = 'FieldTextComponent';
export const FieldText = styled(FieldTextComponent).withConfig({ displayName: "FieldText", componentId: "sc-1diqjx1" }) ``;
//# sourceMappingURL=FieldText.js.map