import React, { forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { useFormContext } from '../../Form';
import { Radio } from '../../Inputs/Radio/Radio';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldRadioComponent = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const { id = uuid() } = props;
    return (React.createElement(Field, Object.assign({ id: id, alignLabel: "right", alignValidationMessage: "right", validationMessage: validationMessage }, pickFieldProps(props)),
        React.createElement(Radio, Object.assign({}, omitFieldProps(props), { id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldRadioComponent.displayName = 'FieldRadioComponent';
export const FieldRadio = styled(FieldRadioComponent).withConfig({ displayName: "FieldRadio", componentId: "sc-1pke9s6" }) ``;
//# sourceMappingURL=FieldRadio.js.map