var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useState, forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { Popover, PopoverContent } from '../../../Popover';
import { InputText } from '../../Inputs/InputText';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
import { FormControl } from '../../FormControl';
import { useFormContext } from '../../Form';
import { polarbrightness2hsv, white, } from './ColorWheel/color_wheel_utils';
import { ColorWheel } from './ColorWheel';
import { LuminositySlider } from './LuminositySlider';
import { Swatch } from './Swatch';
import { hsv2hex, simpleHSVtoFormattedColorString, str2simpleHsv, } from './utils/color_format_utils';
import { isValidColor } from './utils/color_utils';
const colorWheelSize = 164;
const createEventWithHSVValue = (color) => {
    return {
        currentTarget: {
            value: simpleHSVtoFormattedColorString(color),
        },
        target: {
            value: simpleHSVtoFormattedColorString(color),
        },
    };
};
export const FieldColorComponent = forwardRef((_a, ref) => {
    var { alignValidationMessage, hideInput, id = uuid() } = _a, props = __rest(_a, ["alignValidationMessage", "hideInput", "id"]);
    const validationMessage = useFormContext(props);
    const initialValue = props.value && isValidColor(props.value)
        ? str2simpleHsv(props.value)
        : polarbrightness2hsv(white());
    const [color, setColor] = useState(initialValue);
    const [inputTextValue, setInputTextValue] = useState(props.value || '');
    const callOnChange = (newColor) => {
        if (!props.onChange || !newColor)
            return;
        props.onChange(createEventWithHSVValue(newColor));
    };
    const setColorState = (newColor) => {
        setColor(newColor);
        newColor && setInputTextValue(simpleHSVtoFormattedColorString(newColor));
        callOnChange(newColor);
    };
    const handleColorChange = (hs) => setColorState(Object.assign(Object.assign({}, hs), { v: color.v }));
    const handleSliderChange = (event) => setColorState(Object.assign(Object.assign({}, color), { v: Number(event.currentTarget.value) / 100 }));
    const handleInputTextChange = (event) => {
        const value = event.currentTarget.value;
        setInputTextValue(value);
        if (!value || !isValidColor(value))
            return;
        const newColor = str2simpleHsv(value);
        setColor(newColor);
        callOnChange(newColor);
    };
    const content = (React.createElement(PopoverContent, { display: "flex", flexDirection: "column" },
        React.createElement(ColorWheel, { size: colorWheelSize, hue: color.h, saturation: color.s, value: color.v, onColorChange: handleColorChange }),
        React.createElement(LuminositySlider, { min: 0, max: 100, step: 1, value: color.v * 100, width: colorWheelSize, onChange: handleSliderChange })));
    return (React.createElement(Field, Object.assign({ id: id, validationMessage: validationMessage, alignValidationMessage: alignValidationMessage || 'bottom' }, pickFieldProps(props)),
        React.createElement(FormControl, { alignLabel: "left" },
            React.createElement(Popover, { content: content }, (onClick, ref) => (React.createElement(Swatch, { mt: "auto", ref: ref, color: hsv2hex(color), borderRadius: hideInput ? 'medium' : 'none', borderTopLeftRadius: "medium", borderBottomLeftRadius: "medium", borderRight: !hideInput ? 'none' : undefined, width: "33px", height: "28px", onClick: onClick }))),
            !hideInput && (React.createElement(InputText, Object.assign({}, omitFieldProps(props), { id: id, ref: ref, borderRadius: "none", borderTopRightRadius: "medium", borderBottomRightRadius: "medium", validationType: validationMessage && validationMessage.type, onChange: handleInputTextChange, value: inputTextValue }))))));
});
FieldColorComponent.displayName = 'FieldColorComponent';
export const FieldColor = styled(FieldColorComponent).withConfig({ displayName: "FieldColor", componentId: "sc-p8bggy" }) ``;
//# sourceMappingURL=FieldColor.js.map