import React, { forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { useFormContext } from '../../Form';
import { Select } from '../../Inputs/Select/Select';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldSelectComponent = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const { id = uuid() } = props;
    return (React.createElement(Field, Object.assign({ id: id, alignValidationMessage: "bottom", validationMessage: validationMessage }, pickFieldProps(props)),
        React.createElement(Select, Object.assign({}, omitFieldProps(props), { id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldSelectComponent.displayName = 'FieldSelectComponent';
export const FieldSelect = styled(FieldSelectComponent).withConfig({ displayName: "FieldSelect", componentId: "sc-mozilq" }) ``;
//# sourceMappingURL=FieldSelect.js.map