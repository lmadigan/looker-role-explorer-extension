import React, { forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { useFormContext } from '../../Form';
import { Checkbox } from '../../Inputs/Checkbox/Checkbox';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
const FieldCheckboxComponent = forwardRef((props, ref) => {
    const validationMessage = useFormContext(props);
    const { id = uuid() } = props;
    return (React.createElement(Field, Object.assign({ id: id, alignLabel: "left", alignValidationMessage: "right", validationMessage: validationMessage }, pickFieldProps(props)),
        React.createElement(Checkbox, Object.assign({}, omitFieldProps(props), { id: id, validationType: validationMessage && validationMessage.type, ref: ref }))));
});
FieldCheckboxComponent.displayName = 'FieldCheckboxComponent';
export const FieldCheckbox = styled(FieldCheckboxComponent).withConfig({ displayName: "FieldCheckbox", componentId: "sc-1pydtir" }) ``;
//# sourceMappingURL=FieldCheckbox.js.map