import React from 'react';
import styled from 'styled-components';
import { palette } from '@looker/design-tokens';
import { Box } from '../../../Layout/Box';
import { IconButton } from '../../../Button';
import { Text } from '../../../Text';
export const InputSearchControls = ({ onClear, showClear, summary, }) => {
    const clear = (React.createElement(FadeIconButton, { color: "neutral", size: "xsmall", icon: "Close", pr: "xsmall", label: "Clear Filter", show: showClear, onClick: onClear }));
    const separator = (React.createElement(Box, { style: {
            borderRight: `1px solid ${palette.charcoal200}`,
            pointerEvents: 'none',
        }, height: "1.5rem" }));
    return (React.createElement(Box, { mx: "xxsmall", height: "100%", display: "flex", alignItems: "center" },
        summary && (React.createElement(Text, { pr: "xsmall", variant: "subdued", fontSize: "small", style: { pointerEvents: 'none', whiteSpace: 'nowrap' } }, summary)),
        summary && clear && separator,
        clear));
};
const FadeIconButton = styled(IconButton).withConfig({ displayName: "FadeIconButton", componentId: "sc-5a33bt" }) `
  opacity: ${props => (props.show ? 1 : 0)};
  transition: ${props => `opacity ${props.theme.transitions.durationModerate} ${props.theme.easings.ease};`};
`;
//# sourceMappingURL=InputSearchControls.js.map