var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { cloneElement, Children, forwardRef, isValidElement, } from 'react';
import { Box } from '../Layout';
export const ButtonSet = forwardRef((_a, ref) => {
    var { children, disabled, isToggle, name, onChange: groupOnChange, value } = _a, props = __rest(_a, ["children", "disabled", "isToggle", "name", "onChange", "value"]);
    const clonedChildren = Children.map(children, child => {
        if (!isValidElement(child))
            return child;
        const { props: childProps } = child;
        const childValue = childProps.value ||
            (typeof childProps.children === 'string' ? childProps.children : null);
        return cloneElement(child, Object.assign(Object.assign(Object.assign({ isControlled: groupOnChange !== undefined, name, type: isToggle ? 'radio' : 'checkbox', value: childValue }, (value && value.length !== 0
            ? {
                selected: isToggle
                    ? value === childValue
                    : value.indexOf(childValue) > -1,
            }
            : { selected: childProps.selected })), (disabled ? { disabled } : {})), (groupOnChange
            ? {
                onChange: (e) => {
                    groupOnChange && groupOnChange(e);
                    childProps.onChange && childProps.onChange(e);
                },
            }
            : {})));
    });
    return (React.createElement(Box, Object.assign({ alignItems: "center", display: "inline-flex", textAlign: "center", fontSize: "small", ref: ref }, props), clonedChildren));
});
ButtonSet.displayName = 'ButtonSet';
//# sourceMappingURL=ButtonSet.js.map