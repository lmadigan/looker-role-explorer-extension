import styled from 'styled-components';
import { textTransform, } from '@looker/design-tokens';
import { TextBase } from './TextBase';
import { textVariant } from './text_variant';
import { truncate } from './truncate';
const headingLevelFontSize = (props) => {
    switch (props.as) {
        case 'h1':
            return 'xxlarge';
        case 'h3':
            return 'large';
        case 'h4':
            return 'medium';
        case 'h5':
            return 'small';
        case 'h6':
            return 'xsmall';
        case 'h2':
        default:
            return 'xlarge';
    }
};
const headingLineHeight = (props) => props.fontSize ? props.fontSize : headingLevelFontSize(props);
export const Heading = styled(TextBase).attrs((props) => ({
    fontSize: props.fontSize || headingLevelFontSize(props),
    lineHeight: props.lineHeight || headingLineHeight(props),
})).withConfig({ displayName: "Heading", componentId: "sc-cg3kf8" }) `
  ${textTransform}
  ${textVariant}
  ${truncate}
`;
Heading.defaultProps = { as: 'h2', fontWeight: 'normal', variant: 'default' };
//# sourceMappingURL=Heading.js.map