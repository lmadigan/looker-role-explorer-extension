import React from 'react';
import { palette } from '@looker/design-tokens';
import { Box } from '../../Layout/Box';
import { IconButton } from '../../Button';
import { Text } from '../../Text';
export var MenuSearchControls = function (_a) {
    var onClear = _a.onClear, hasValue = _a.hasValue, summary = _a.summary;
    var clear = onClear && hasValue && (React.createElement(IconButton, { color: "neutral", size: "small", icon: "Close", label: "Clear Filter", onClick: onClear }));
    var separator = (React.createElement(Box, { mr: "small", ml: "medium", style: {
            borderRight: "1px solid " + palette.charcoal200,
            pointerEvents: 'none',
        }, height: "1.5rem" }));
    return (React.createElement(Box, { position: "absolute", top: "0", right: "0", mr: "large", height: "100%", display: "flex", alignItems: "center" },
        summary && (React.createElement(Text, { variant: "subdued", fontSize: "small", style: { pointerEvents: 'none' } }, summary)),
        summary && clear && separator,
        clear));
};
//# sourceMappingURL=MenuSearchControls.js.map