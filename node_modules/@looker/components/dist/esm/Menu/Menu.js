import React, { Children, cloneElement, useRef, useState, } from 'react';
export function useMenu(disabled, initialIsOpen) {
    if (initialIsOpen === void 0) { initialIsOpen = false; }
    var triggerRef = useRef(null);
    var _a = useState(initialIsOpen), isOpen = _a[0], setOpen = _a[1];
    return { disabled: disabled, isOpen: isOpen, setOpen: setOpen, triggerRef: triggerRef };
}
export var Menu = function (_a) {
    var children = _a.children, disabled = _a.disabled, isOpen = _a.isOpen;
    var menu = useMenu(disabled, isOpen);
    var cloned = Children.map(children, function (child) {
        return cloneElement(child, menu);
    });
    return React.createElement(React.Fragment, null, cloned);
};
//# sourceMappingURL=Menu.js.map