export var getModalRoot = function () {
    var existing = document.getElementById('modal-root');
    if (existing) {
        return existing;
    }
    else {
        var newElement = document.createElement('div');
        newElement.id = 'modal-root';
        document.body.appendChild(newElement);
        return newElement;
    }
};
//# sourceMappingURL=modalRoot.js.map