import React from 'react';
import { CSSTransition } from 'react-transition-group';
import { ModalBackdrop } from './ModalBackdrop';
import { ModalContext } from './ModalContext';
import { ModalPortal } from './ModalPortal';
export var CustomizableModalAttributes = {
    backdrop: { backgroundColor: 'palette.charcoal200', opacity: 0.6 },
};
export function Modal(_a) {
    var backdrop = _a.backdrop, isOpen = _a.isOpen, onClose = _a.onClose, portalRef = _a.portalRef, render = _a.render;
    return (React.createElement(ModalContext.Provider, { value: { closeModal: onClose } },
        React.createElement(CSSTransition, { classNames: "modal", mountOnEnter: true, unmountOnExit: true, in: isOpen, timeout: { enter: 0, exit: 250 } }, function (state) { return (React.createElement(ModalPortal, { portalRef: portalRef },
            React.createElement(ModalBackdrop, { className: state, onClick: onClose, visible: backdrop === undefined ? true : !!backdrop, style: !!backdrop && backdrop !== true
                    ? backdrop
                    : undefined }),
            render(state))); })));
}
//# sourceMappingURL=Modal.js.map