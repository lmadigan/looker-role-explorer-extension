var __makeTemplateObject = (this && this.__makeTemplateObject) || function (cooked, raw) {
    if (Object.defineProperty) { Object.defineProperty(cooked, "raw", { value: raw }); } else { cooked.raw = raw; }
    return cooked;
};
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useState, forwardRef } from 'react';
import styled from 'styled-components';
import uuid from 'uuid/v4';
import { Popover, PopoverContent } from '../../../Popover';
import { InputText } from '../../Inputs/InputText';
import { Field, omitFieldProps, pickFieldProps } from '../Field';
import { FormControl } from '../../FormControl';
import { useFormContext } from '../../Form';
import { polarbrightness2hsv, white, } from './ColorWheel/color_wheel_utils';
import { ColorWheel } from './ColorWheel';
import { LuminositySlider } from './LuminositySlider';
import { Swatch } from './Swatch';
import { hsv2hex, simpleHSVtoFormattedColorString, str2simpleHsv, } from './utils/color_format_utils';
import { isValidColor } from './utils/color_utils';
var colorWheelSize = 164;
var createEventWithHSVValue = function (color) {
    return {
        currentTarget: {
            value: simpleHSVtoFormattedColorString(color),
        },
        target: {
            value: simpleHSVtoFormattedColorString(color),
        },
    };
};
export var FieldColorComponent = forwardRef(function (_a, ref) {
    var alignValidationMessage = _a.alignValidationMessage, hideInput = _a.hideInput, _b = _a.id, id = _b === void 0 ? uuid() : _b, props = __rest(_a, ["alignValidationMessage", "hideInput", "id"]);
    var validationMessage = useFormContext(props);
    var initialValue = props.value && isValidColor(props.value)
        ? str2simpleHsv(props.value)
        : polarbrightness2hsv(white());
    var _c = useState(initialValue), color = _c[0], setColor = _c[1];
    var _d = useState(props.value || ''), inputTextValue = _d[0], setInputTextValue = _d[1];
    var callOnChange = function (newColor) {
        if (!props.onChange || !newColor)
            return;
        props.onChange(createEventWithHSVValue(newColor));
    };
    var setColorState = function (newColor) {
        setColor(newColor);
        newColor && setInputTextValue(simpleHSVtoFormattedColorString(newColor));
        callOnChange(newColor);
    };
    var handleColorChange = function (hs) {
        return setColorState(__assign(__assign({}, hs), { v: color.v }));
    };
    var handleSliderChange = function (event) {
        return setColorState(__assign(__assign({}, color), { v: Number(event.currentTarget.value) / 100 }));
    };
    var handleInputTextChange = function (event) {
        var value = event.currentTarget.value;
        setInputTextValue(value);
        if (!value || !isValidColor(value))
            return;
        var newColor = str2simpleHsv(value);
        setColor(newColor);
        callOnChange(newColor);
    };
    var content = (React.createElement(PopoverContent, { display: "flex", flexDirection: "column" },
        React.createElement(ColorWheel, { size: colorWheelSize, hue: color.h, saturation: color.s, value: color.v, onColorChange: handleColorChange }),
        React.createElement(LuminositySlider, { min: 0, max: 100, step: 1, value: color.v * 100, width: colorWheelSize, onChange: handleSliderChange })));
    return (React.createElement(Field, __assign({ id: id, validationMessage: validationMessage, alignValidationMessage: alignValidationMessage || 'bottom' }, pickFieldProps(props)),
        React.createElement(FormControl, { alignLabel: "left" },
            React.createElement(Popover, { content: content }, function (onClick, ref) { return (React.createElement(Swatch, { mt: "auto", ref: ref, color: hsv2hex(color), borderRadius: hideInput ? 'medium' : 'none', borderTopLeftRadius: "medium", borderBottomLeftRadius: "medium", borderRight: !hideInput ? 'none' : undefined, width: "33px", height: "28px", onClick: onClick })); }),
            !hideInput && (React.createElement(InputText, __assign({}, omitFieldProps(props), { id: id, ref: ref, borderRadius: "none", borderTopRightRadius: "medium", borderBottomRightRadius: "medium", validationType: validationMessage && validationMessage.type, onChange: handleInputTextChange, value: inputTextValue }))))));
});
FieldColorComponent.displayName = 'FieldColorComponent';
export var FieldColor = styled(FieldColorComponent).withConfig({ displayName: "FieldColor", componentId: "sc-p8bggy" })(templateObject_1 || (templateObject_1 = __makeTemplateObject([""], [""])));
var templateObject_1;
//# sourceMappingURL=FieldColor.js.map