"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var getTabStops = function (ref) {
    return Array.from(ref.querySelectorAll('a,button,[tabindex="0"]'));
};
exports.moveFocus = function (direction, initial, containerRef) {
    if (!containerRef || !containerRef.current)
        return;
    var tabStops = getTabStops(containerRef.current);
    if (document.activeElement &&
        tabStops.includes(document.activeElement)) {
        var next = tabStops.findIndex(function (f) { return f === document.activeElement; }) + direction;
        if (next === tabStops.length)
            return;
        if (!tabStops[next])
            return;
        tabStops[next].focus();
    }
    else {
        tabStops.slice(initial)[0].focus();
    }
    return false;
};
//# sourceMappingURL=moveFocus.js.map