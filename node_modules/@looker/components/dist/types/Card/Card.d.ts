import { CompatibleHTMLProps, FlexboxProps } from '@looker/design-tokens';
import { LayoutComponentProps } from '../Layout/layout';
export interface CardProps extends CompatibleHTMLProps<HTMLElement>, FlexboxProps, LayoutComponentProps {
    raised?: boolean;
}
export declare const Card: import("styled-components").StyledComponent<"div", any, CardProps, never>;
