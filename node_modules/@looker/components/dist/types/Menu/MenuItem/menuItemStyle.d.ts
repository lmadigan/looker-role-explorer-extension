import { FontWeights, FontSizes } from '@looker/design-tokens';
export interface MenuItemStateStyle {
    bg: string;
    color: string;
    fontWeight: FontWeights;
    fontSize: FontSizes;
    iconColor: string;
    iconSize: number;
}
export interface MenuItemStyle {
    current: MenuItemStateStyle;
    hover: MenuItemStateStyle;
    initial: MenuItemStateStyle;
    marker: {
        color: string;
        size: number;
    };
}
export declare const defaultMenuItemStyle: MenuItemStyle;
