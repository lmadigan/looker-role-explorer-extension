import { CompatibleHTMLProps, ColorProps, SpaceProps, TypographyProps } from '@looker/design-tokens';
import { IconNames } from '@looker/icons';
import { FC, ReactNode } from 'react';
import { MenuItemCustomization } from './menuItemCustomization';
export interface MenuSharedProps {
    customizationProps?: MenuItemCustomization;
    compact?: boolean;
}
export interface MenuItemProps extends CompatibleHTMLProps<HTMLElement>, ColorProps, SpaceProps, TypographyProps, MenuSharedProps {
    detail?: ReactNode;
    icon?: IconNames;
    current?: boolean;
    itemRole?: 'link' | 'button';
}
export declare const MenuItem: FC<MenuItemProps>;
