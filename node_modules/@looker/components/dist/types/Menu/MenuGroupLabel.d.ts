import { FC, CSSProperties, ReactNode } from 'react';
import { BackgroundColorProps, BorderRadiusProps } from 'styled-system';
import { HeadingProps } from '../Text/Heading';
interface MenuGroupLabelProps extends BackgroundColorProps, HeadingProps, BorderRadiusProps {
    labelContent: ReactNode;
    labelStyles?: CSSProperties;
}
export declare const MenuGroupLabel: FC<MenuGroupLabelProps>;
export {};
