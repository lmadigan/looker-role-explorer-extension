import { CompatibleHTMLProps } from '@looker/design-tokens';
import { FC } from 'react';
import { CSSObject } from 'styled-components';
import { BackgroundColorProps, BorderProps, BoxShadowProps, LayoutProps } from 'styled-system';
export interface ModalSurfaceProps extends CompatibleHTMLProps<HTMLDivElement>, BorderProps, BoxShadowProps, BackgroundColorProps, LayoutProps {
    surfaceStyle?: CSSObject;
    anchor?: 'right';
    animationState?: string;
}
export declare const ModalSurface: FC<ModalSurfaceProps>;
