import { SpaceProps, CompatibleHTMLProps } from '@looker/design-tokens';
import { IconNames } from '@looker/icons';
import { FC } from 'react';
export interface ModalHeaderProps extends SpaceProps, CompatibleHTMLProps<HTMLElement> {
    closeIcon?: IconNames;
}
export declare const ModalHeader: FC<ModalHeaderProps>;
