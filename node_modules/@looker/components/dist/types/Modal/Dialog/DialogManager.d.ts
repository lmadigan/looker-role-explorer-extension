/// <reference types="react" />
import { ManagedModalProps } from '../Modal';
import { ModalManager } from '../ModalManager';
export declare class DialogManager extends ModalManager {
    protected renderModal(content: JSX.Element, props: ManagedModalProps): JSX.Element;
}
