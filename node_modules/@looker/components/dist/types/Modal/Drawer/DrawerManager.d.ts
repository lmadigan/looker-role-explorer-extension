import { ReactNode } from 'react';
import { ManagedModalProps } from '../Modal';
import { ModalManager } from '../ModalManager';
export declare class DrawerManager extends ModalManager {
    protected renderModal(content: ReactNode, props: ManagedModalProps): JSX.Element;
}
