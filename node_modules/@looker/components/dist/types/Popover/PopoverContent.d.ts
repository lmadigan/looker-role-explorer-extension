import { FlexboxProps, SpaceProps } from '@looker/design-tokens';
import { LayoutProps } from 'styled-system';
interface PopoverContent extends SpaceProps, LayoutProps, FlexboxProps {
}
export declare const PopoverContent: import("styled-components").StyledComponent<"div", any, PopoverContent, never>;
export {};
