import { BorderProps, BoxShadowProps } from '@looker/design-tokens';
import { Placement } from 'popper.js';
import React, { CSSProperties, DOMAttributes, ReactNode } from 'react';
import { PopperArrowProps } from 'react-popper';
export interface SurfaceStyleProps extends BorderProps, BoxShadowProps {
    color?: string;
    backgroundColor?: string;
    border?: string;
    borderColor?: string;
}
export interface OverlaySurfaceProps extends SurfaceStyleProps {
    arrow?: boolean;
    arrowProps: PopperArrowProps;
    children: ReactNode;
    eventHandlers?: DOMAttributes<{}>;
    placement: Placement;
    style?: CSSProperties;
    zIndex?: number;
}
export declare const OverlaySurface: React.ForwardRefExoticComponent<OverlaySurfaceProps & React.RefAttributes<HTMLDivElement>>;
