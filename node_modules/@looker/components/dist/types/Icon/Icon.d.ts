import { CompatibleHTMLProps, LayoutProps, SpaceProps } from '@looker/design-tokens';
import React from 'react';
import { IconNames } from '@looker/icons';
export interface IconProps extends Omit<CompatibleHTMLProps<HTMLDivElement>, 'onClick'>, LayoutProps, SpaceProps {
    color?: string;
    name: IconNames;
}
export { IconNames };
export declare const Icon: import("styled-components").StyledComponent<React.ForwardRefExoticComponent<IconProps & React.RefAttributes<HTMLDivElement>>, any, {}, never>;
