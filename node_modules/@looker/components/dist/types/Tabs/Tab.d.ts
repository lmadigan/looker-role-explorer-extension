import { ReactNode } from 'react';
import { SpaceProps, LayoutProps, BorderProps, TypographyProps } from '@looker/design-tokens';
export interface TabProps extends BorderProps, LayoutProps, SpaceProps, TypographyProps {
    children: ReactNode;
    selected?: boolean;
    disabled?: boolean;
    onSelect?: () => void;
}
export declare const Tab: import("styled-components").StyledComponent<"button", any, {
    minWidth: string;
    onClick: () => void;
} & TabProps, "onClick" | "minWidth">;
