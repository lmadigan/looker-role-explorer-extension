import { IApiSettings } from '@looker/sdk/dist/rtl/apiSettings';
import { IAuthorizer, IRequestInit, ITransport } from '@looker/sdk/dist/rtl/transport';
export declare class ExtensionSession implements IAuthorizer {
    settings: IApiSettings;
    sudoId: string;
    transport: ITransport;
    constructor(settings: IApiSettings, transport: ITransport);
    isAuthenticated(): boolean;
    authenticate(init: IRequestInit): Promise<IRequestInit>;
    isSudo(): boolean;
    logout(): Promise<boolean>;
    reset(): void;
}
